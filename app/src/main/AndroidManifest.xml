<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tvmazeapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TVMazeApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVMazeApp">
        <activity
            android:name=".presentation.views.CustomSettingsActivity"
            android:label="@string/title_activity_settings"
            android:exported="false" />
        <activity
            android:name=".presentation.views.UnlockActivity"
            android:exported="false"
            android:label="@string/unlock_title" />
        <activity
            android:name=".presentation.views.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.views.ShowsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:resizeableActivity="true"
            tools:targetApi="24">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>